query allChannels {
  channels(orderBy: "nodeId", orderDirection: "desc") {
    hashId
    id
    uri {
      id
      imageUri
      description
      name
    }
    nodeId
    items {
      chainId
      id
      target
      targetId
      userId
      hasId
      createdAt
    }
  }
}