query allChannels {
  channels(orderBy: "nodeId", orderDirection: "desc") {
    hashId
    id
    uri
    nodeId
    items {
      chainId
      id
      target
      targetId
      userId
      hasId
      createdAt
    }
    name
  }
}