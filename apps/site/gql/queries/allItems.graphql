query allItems {
  items(orderBy: "id", orderDirection: "desc") {
    creatorId
    id
    type
    timestamp
    target {
      id
      type
      publication {
        creatorId
        id
        timestamp
        uri
      }
    }
  }
}