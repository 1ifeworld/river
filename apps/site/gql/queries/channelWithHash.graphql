query ChannelWithHash($hashId: String!) {
  channels(where: { hashId: $hashId }) {
    hashId
    id
    uri {
      id
      imageUri
      description
      name
    }
    items {
      chainId
      id
      target
      targetId
      userId
      createdAt
    }
  }
}
