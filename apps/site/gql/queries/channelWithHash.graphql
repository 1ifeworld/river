query ChannelWithHash($hashId: String!) {
  channels(where: { hashId: $hashId }) {
    hashId
    id
    name
    description
    coverImageURI
    items {
      chainId
      id
      target
      targetId
      userId
      createdAt
    }
  }
}
