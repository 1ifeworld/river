query ChannelWithHash($hashId: String!) {
  channels(where: { hashId: $hashId }) {
    hashId
    id
    uri {
      id
      imageUri
      description
      name
    }
    nodeId
    items {
      chainId
      id
      target
      targetId
      userId
      hasId
      createdAt
    }
  }
}
