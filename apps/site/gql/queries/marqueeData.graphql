query marqueeData {
  addss(limit: 75, orderBy: "timestamp", orderDirection: "desc") {
    items {
      removed
      addedById      
      item {
        id
        uri
      }
      channel {
        id
        name  
      }
    }
  }
}