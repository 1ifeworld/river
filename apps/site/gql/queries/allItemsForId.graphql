query allItemsForId($creatorId: BigInt) {
  items(orderBy: "timestamp", orderDirection: "desc", where: {creatorId: $creatorId}) {
    creatorId
    id
    type
    timestamp
    target {
      id
      type
      publication {
        creatorId
        id
        timestamp
        uri
      }
    }
  }
}