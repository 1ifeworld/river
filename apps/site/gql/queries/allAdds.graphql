query allAdds {
  addss(orderBy: "timestamp", orderDirection: "desc") {
    items {
      addedBy
      item {
        id
        timestamp
        createdby
        uri
      }
      channelId
      channel {
        name
        adds(orderBy: "timestamp", orderDirection: "desc") {
          items {
            itemId
          }
        }
      }
    }
  }
}