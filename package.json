{
  "name": "river",
  "version": "0.0.0",
  "author": "Lifeworld",
  "license": "MIT",
  "description": "Set information free",
  "main": "index.js",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "turbo start",
    "lint": "turbo lint",
    "format": "turbo format",
    "start:delta": "pnpm build --filter=offchain-schema && pnpm start --filter=delta",
    "build:site": "pnpm build --filter=site",
    "dev:site": "pnpm dev --filter=site"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.3.1",
    "turbo": "^1.10.16"
  },
  "dependencies": {
    "offchain-schema": "workspace:^"
  }
}
